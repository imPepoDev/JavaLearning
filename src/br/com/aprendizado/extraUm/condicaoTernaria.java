package br.com.aprendizado.extraUm;

public class condicaoTernaria {
    public static void main(String args[]) {

        double preco = 34.5;

        /*
        *
        * Dica para ler essa condição ternária:
        * exemplo;
        * desconto ()
        * o Parenteses() será reconhecido como if
        * desconto (preco < 20.0)
        * if (preco <20)
        *
        * desconto (preco < 20.0) ?
        * O ponto de interrogação será reconhecido como as chaves{}, ou algo como: Minha primeira ação.
        * desconto (preco < 20.0) ? preco * 0.1
        * if (preco < 20.0) {
        *   desconto = preco * 0.1
        * }
        *
        * desconto (preco < 20.0) ? preco * 0.1 :
        * Os dois pontos ( : ) será reconhecido como else, ou algo como: Se não for a primeira coisa, tente isso.
        * desconto (preco < 20.0) ? preco * 0.1 : preco * 0.05
        * if (preco < 20.0) {
        *   desconto = preco * 0.1
        * }
        *
        * else {
        *   desconto = preco * 0.05
        * }
        *
        *
        * Deu pra entender mais ou menos? kk
        *
        * */

    }
}
